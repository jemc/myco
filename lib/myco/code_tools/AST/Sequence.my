
CodeTools::AST << {
  
  Sequence < Node {
    var array
    
    Form: [:sequence, :array]
    
    bytecode: |g| {
      ary = self.array.dup
      last = ary.pop
      
      ary.each |item| { item.bytecode(g); g.pop }
      last.bytecode(g)
    }
  }
  
}
