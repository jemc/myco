
CodeTools::AST << {
  
  Body < Node, BasicObject {
    var array
    
    Form: [:block, :array]
    
    bytecode: |g| {
      ary = self.array.dup
      last = ary.pop
      
      ary.each |item| { item.bytecode(g); g.pop }
      last.bytecode(g)
    }
  }
  
}
