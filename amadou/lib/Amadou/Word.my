
Amadou << {
  
  Word: BasicObject {
    const local_regexp: Regexp.new("^([[:lower:]]|_)")
    local?: |token| !!local_regexp.match(token.text)
    
    const integer_regexp: Regexp.new("^[0-9]+$")
    integer?: |token| !!integer_regexp.match(token.text)
  }
  
}
